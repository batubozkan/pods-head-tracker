# Everything deployed to the Pi must stay LF: CRLF breaks bash scripts,
# python shebangs, and systemd EnvironmentFile values on Linux — even when
# the repo is cloned on Windows.
* text=auto
*.py text eol=lf
*.sh text eol=lf
*.service text eol=lf
*.example text eol=lf
